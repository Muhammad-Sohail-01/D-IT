<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <!-- <record id="action_report_car_invoice" model="ir.actions.report">
            <field name="name">Tax Invoice</field>
            <field name="model">account.move</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">invoice_extend.report_car_invoice</field>
            <field name="report_file">invoice_extend.report_car_invoice</field>
            <field name="print_report_name">('Invoice - %s' % (object.name))</field>
            <field name="binding_model_id" ref="model_account_move"/>
            <field name="binding_type">report</field>
        </record>-->
        
        <!-- QWeb Reports -->
        <record id="account.account_invoices" model="ir.actions.report">
            <field name="name">Tax Invoice</field>
            <field name="model">account.move</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">invoice_extend.report_invoice_with_payments</field>
            <field name="report_file">invoice_extend.report_invoice_with_payments</field>
            <field name="attachment">(object.state == 'posted') and ((object.name or 'INV').replace('/','_')+'.pdf')</field>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="binding_type">report</field>
            <field name="groups_id" eval="[(4, ref('account.group_account_invoice')),
 (4, ref('account.group_account_readonly'))]"/>
        </record> 
        
    </data>
</odoo>
